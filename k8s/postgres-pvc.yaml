# Postgres 数据卷（单节点 RWO 即可）
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: ns-os8fj70c
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi